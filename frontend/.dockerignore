# Without this, `COPY . .` copies the HOST's node_modules over the Linux one
# that `npm ci` just built in the image — which breaks any dependency shipping
# a platform-native binary.
node_modules
.next
npm-debug.log
